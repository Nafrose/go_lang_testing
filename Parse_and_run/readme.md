# Parser Run to Open Process

```bash
go run .\parseAndRun.go -arg="run cmd"
go run .\parseAndRun.go -arg="run powershell"
go run .\parseAndRun.go -arg="run chrome"
```

- cmd opens CMD
- powershell opens powershell
- chrome opens a new chrome window
